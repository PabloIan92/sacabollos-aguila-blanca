---
phase: 02-caso-de-seguro
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/routes.ts
  - src/app/AppRouter.tsx
  - src/features/recepcion/RecepcionHome.tsx
  - src/features/casos/CasoNuevoPage.tsx
  - src/features/casos/CasoNuevoPage.test.tsx
  - src/features/casos/CasosListPage.tsx
  - src/features/casos/CasosListPage.test.tsx
  - src/features/casos/FichaInspeccionPage.tsx
  - src/features/casos/FichaInspeccionPage.test.tsx
  - src/features/casos/components/DamageCheckboxes.tsx
  - src/features/casos/components/FotoUploader.tsx
autonomous: true
requirements: [CASOS-01, FICHAS-01, FICHAS-04]

estimate:
  tokens: 62000
  raw_tokens: 62000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Recepción crea un caso de canal Seguro completando cliente, patente, aseguradora (de las 6 fijas), número de siniestro, denuncia y productor/asesor, y el caso queda guardado con estado 'borrador' (CASOS-01, D-18, D-19)"
    - "El item de menú Casos de recepción, marcado available:false desde la Fase 1, ahora navega a un listado real de casos en /casos"
    - "Después de crear el caso, el flujo lleva a la Ficha de inspección pre-ingreso, que exige marcar al menos una zona dañada por checkbox (D-17) y subir las 4 fotos obligatorias (frente, atrás, lateral izquierdo, lateral derecho) antes de habilitar el botón de guardar (D-16, FICHAS-01, FICHAS-04)"
    - "El botón 'Marcar como enviado a la aseguradora' (D-15) solo se habilita después de guardar la ficha de inspección, y al presionarlo el caso pasa de 'borrador' a 'enviado a la aseguradora' sin que el sistema mande ningún mail"
    - "El botón 'Nuevo caso' de RecepcionHome, deshabilitado desde la Fase 1, ahora navega a la creación de caso"
  artifacts:
    - path: "src/features/casos/CasoNuevoPage.tsx"
      provides: "formulario de alta de caso de canal Seguro"
      exports: ["CasoNuevoPage"]
    - path: "src/features/casos/CasosListPage.tsx"
      provides: "listado de casos para recepción en /casos"
      exports: ["CasosListPage"]
    - path: "src/features/casos/FichaInspeccionPage.tsx"
      provides: "Ficha de inspección pre-ingreso con zonas dañadas + 4 fotos obligatorias + acción de marcar enviado"
      exports: ["FichaInspeccionPage"]
    - path: "src/features/casos/components/DamageCheckboxes.tsx"
      provides: "checklist fijo de 10 zonas del auto (D-17)"
      exports: ["DamageCheckboxes"]
    - path: "src/features/casos/components/FotoUploader.tsx"
      provides: "dropzone de 4 ángulos con preview y bloqueo de guardado por debajo de 4 fotos"
      exports: ["FotoUploader"]
  key_links:
    - from: "src/features/casos/CasoNuevoPage.tsx"
      to: "src/features/casos/api.ts"
      via: "createCaso(...) del plan 02-01, con canal fijo 'seguro'"
      pattern: "createCaso"
    - from: "src/features/casos/FichaInspeccionPage.tsx"
      to: "src/features/casos/hooks/useCasoFotos.ts"
      via: "uploadFoto/listFotos del plan 02-01 para los 4 ángulos sin prefijo"
      pattern: "useCasoFotos"
    - from: "src/features/casos/FichaInspeccionPage.tsx"
      to: "src/features/casos/api.ts"
      via: "updateCasoEstado(id, 'enviado a la aseguradora') al presionar 'Marcar como enviado a la aseguradora'"
      pattern: "updateCasoEstado"
    - from: "src/app/routes.ts"
      to: "src/features/casos/CasosListPage.tsx"
      via: "el item Casos de recepción pasa a available:true y apunta a /casos"
      pattern: "available: true"
---

<objective>
Dar de alta un caso de Seguro y completar su Ficha de inspección pre-ingreso: recepción entra los datos de denuncia/siniestro/productor, marca las zonas dañadas y sube las 4 fotos obligatorias, y desde ahí marca el caso como enviado a la aseguradora.

Purpose: cierra el primer tramo del flujo de punta a punta de la fase — el caso nace y llega hasta el punto donde recepción manda el mail a mano y espera la orden de trabajo. Es también la primera vez que el item de menú "Casos" de recepción, deshabilitado desde la Fase 1, cobra vida.
Output: alta de caso, listado de casos de recepción, y la Ficha de inspección pre-ingreso con su bloqueo de guardado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-caso-de-seguro/02-CONTEXT.md
@.planning/phases/01-fundaciones/01-02-SUMMARY.md
@.planning/phases/01-fundaciones/01-03-SUMMARY.md
@docs/flujos-originales/TRANSCRIPCION.md
</context>

<interfaces>
De `src/features/casos/types.ts` y `api.ts` (plan 02-01, ya implementados):
- `Caso`, `CasoEstado`, `ESTADOS_CASO`, `ZonaDano`, `ZONAS_DANO` (10 zonas), `AnguloFoto`, `ANGULOS_FOTO` (4 ángulos)
- `createCaso(datos)`, `getCaso(id)`, `listCasos()`, `updateCasoEstado(id, estado, extra?)`
- `useCasoFotos()` → `{ uploadFoto(caseId, angulo, file), listFotos(caseId, angulos) }`
- `buildFotoPath(caseId, angulo)` de `src/lib/imageCompression.ts`

De `src/app/routes.ts` (Fase 1, ya implementado):
- `navItems` incluye `{ to: '/casos', label: 'Casos', icon: ClipboardList, roles: ['recepcion'], available: false }` — este plan cambia esa bandera a `true`.

De `src/ui/` (Fase 1): `Ficha`, `TextField`, `PrimaryButton` (props: `error?`, `loading?`), `EmptyState`.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Alta de caso de Seguro + listado de casos de recepción</name>
  <read_first>
    - `.planning/phases/02-caso-de-seguro/02-CONTEXT.md` D-18 (6 aseguradoras fijas), D-19 (productor texto libre)
    - `docs/flujos-originales/TRANSCRIPCION.md` líneas 20-32 (campos de la Ficha 1: datos del auto, datos de la persona, seguro, siniestro, productor)
    - `src/app/routes.ts` (el item Casos de recepción a habilitar)
    - `src/app/AppRouter.tsx` y `src/features/recepcion/RecepcionHome.tsx` de la Fase 1 (dónde engancha la ruta nueva y el botón "Nuevo caso" deshabilitado)
    - `src/ui/TextField.tsx`, `src/ui/PrimaryButton.tsx` (las primitivas que compone el formulario)
  </read_first>
  <files>src/app/routes.ts, src/app/AppRouter.tsx, src/features/recepcion/RecepcionHome.tsx, src/features/casos/CasoNuevoPage.tsx, src/features/casos/CasoNuevoPage.test.tsx, src/features/casos/CasosListPage.tsx, src/features/casos/CasosListPage.test.tsx</files>
  <behavior>
    - El formulario de alta no permite guardar si falta `patente`, `cliente_nombre`, `cliente_telefono`, `aseguradora`, `numero_siniestro` o `denuncia` — el botón de submit queda deshabilitado.
    - `aseguradora` es un `<select>` con exactamente las 6 opciones de D-18, sin opción de texto libre.
    - Al guardar con éxito, llama a `createCaso` una sola vez y navega a `/casos/{id}/ficha-inspeccion`.
    - `/casos` (recepción) lista los casos de `listCasos()` mostrando patente, cliente y estado; si no hay casos, muestra el `EmptyState` ya existente con la misma copy del contrato de Fase 1 adaptada a "casos" en general.
    - Un click en una fila de `/casos` navega a `/casos/{id}` (detalle, que el plan 02-03 completa) o directo a la ficha si el caso sigue en `'borrador'`.
  </behavior>
  <action>
    En `src/app/routes.ts`, cambiá `available: false` a `available: true` en el item `{ to: '/casos', label: 'Casos', roles: ['recepcion'] }`. No toques ningún otro item.

    Escribí `src/features/casos/CasoNuevoPage.tsx`: formulario con `TextField` para `cliente_nombre`, `cliente_telefono`, `patente`, `marca`, `modelo`, `color` (estos 3 últimos opcionales), un `<select>` nativo para `aseguradora` con las 6 opciones exactas de D-18, `TextField` para `numero_siniestro` y `denuncia` (este último puede ser `textarea` simple si `TextField` no soporta multilínea — usá un `<textarea>` con las mismas clases de borde/foco que `TextField` si hace falta texto largo), y `TextField` para `productor_nombre`/`productor_telefono` (D-19, opcionales). El canal queda fijo en `'seguro'`: no renderices un selector de canal en esta fase — Particular es CASOS-03 de la Fase 3, y mostrar una opción que no lleva a ningún lado repetiría el anti-patrón que la Fase 1 ya evitó con los items de menú no disponibles. Al submit, llamá a `createCaso` con `created_by` resuelto de `useAuth().session.user.id`, y navegá a la Ficha de inspección del caso recién creado.

    Escribí `src/features/casos/CasosListPage.tsx`: usa `listCasos()` en un efecto al montar, renderiza una tabla o lista de filas (patente, cliente, aseguradora, estado como texto plano — el semáforo visual llega en el plan 02-04, en esta task alcanza con el string de `estado`), con el `EmptyState` para el caso de lista vacía y un botón "Nuevo caso" que navega a `/casos/nuevo`. Cada fila es un link a `/casos/{id}` si el caso ya no está en `'borrador'`, o a `/casos/{id}/ficha-inspeccion` si sigue en `'borrador'` (todavía no tiene su ficha completa).

    En `src/app/AppRouter.tsx`, agregá dentro del subárbol protegido de `recepcion` las rutas `/casos` (`CasosListPage`), `/casos/nuevo` (`CasoNuevoPage`) y `/casos/:id/ficha-inspeccion` (placeholder de la task 2, `FichaInspeccionPage`). Usá el mismo patrón de `RequireRole` que ya protege el resto del árbol, con `roles={['recepcion']}` para estas tres — dueño y taller ven casos desde su propia home (plan 02-04), no desde estas rutas de gestión.

    En `src/features/recepcion/RecepcionHome.tsx`, quitá el `disabled` y el `title="Disponible en la próxima entrega"` del botón "Nuevo caso" y cableálo a `useNavigate()` hacia `/casos/nuevo`. Es el primer control de la Fase 1 que deja de estar deshabilitado: confirmá que efectivamente navega y no se queda como un link muerto.

    Escribí `CasoNuevoPage.test.tsx` y `CasosListPage.test.tsx` cubriendo el bloque `<behavior>`, mockeando `src/features/casos/api.ts`.
  </action>
  <verify>
    <automated>npx vitest run src/features/casos/CasoNuevoPage.test.tsx src/features/casos/CasosListPage.test.tsx && npx tsc --noEmit && npm run build && grep -c "available: true" src/app/routes.ts</automated>
  </verify>
  <acceptance_criteria>
    - Los dos archivos de test pasan, cubriendo el bloque `<behavior>` completo.
    - El `<select>` de aseguradora tiene exactamente 6 `<option>` con los valores de D-18, sin opción "otra" ni texto libre.
    - El botón "Nuevo caso" de `RecepcionHome` ya no tiene `disabled` ni la leyenda de "próxima entrega".
    - `npx tsc --noEmit` y `npm run build` terminan en 0.
  </acceptance_criteria>
  <done>Recepción crea un caso de Seguro desde un formulario real, lo ve en un listado real en /casos, y el botón "Nuevo caso" de su home ya no está deshabilitado.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Ficha de inspección pre-ingreso — zonas dañadas, 4 fotos obligatorias y marcar enviado</name>
  <read_first>
    - `.planning/phases/02-caso-de-seguro/02-CONTEXT.md` D-16 (4 fotos mínimo), D-17 (10 zonas fijas), D-15 (envío manual)
    - `.planning/phases/02-caso-de-seguro/02-RESEARCH.md` §Common Pitfalls 4 (race condition upload→insert) — en este plan el caso ya existe antes de subir fotos (se crea en la task 1), así que no aplica el path temporal que describe esa nota; las fotos suben directo a `casos/{id}/{angulo}.webp`
    - `src/features/casos/CasoNuevoPage.tsx` de la task 1 (a dónde navega después de crear el caso)
    - `src/features/casos/hooks/useCasoFotos.ts` y `src/lib/imageCompression.ts` del plan 02-01 (`uploadFoto`, `listFotos`, `buildFotoPath`)
  </read_first>
  <files>src/features/casos/FichaInspeccionPage.tsx, src/features/casos/FichaInspeccionPage.test.tsx, src/features/casos/components/DamageCheckboxes.tsx, src/features/casos/components/FotoUploader.tsx</files>
  <behavior>
    - `DamageCheckboxes` renderiza exactamente las 10 zonas de `ZONAS_DANO`, ninguna más, ninguna menos, y devuelve el subconjunto marcado.
    - `FotoUploader` renderiza 4 dropzones, una por cada valor de `ANGULOS_FOTO`, cada una con su preview tras subir.
    - El botón "Guardar ficha de inspección" está deshabilitado hasta que las 4 fotos estén subidas (FICHAS-04); las zonas dañadas pueden quedar en cero marcadas (no todo auto con daño tiene 4 zonas, y el checklist no fuerza un mínimo ahí).
    - El botón "Marcar como enviado a la aseguradora" está deshabilitado hasta que la ficha se haya guardado con éxito, y al presionarlo llama a `updateCasoEstado(id, 'enviado a la aseguradora')` una sola vez y no dispara ninguna llamada de red de envío de mail.
  </behavior>
  <action>
    Escribí `src/features/casos/components/DamageCheckboxes.tsx`: recibe `selected: ZonaDano[]` y `onChange(zonas: ZonaDano[])`, renderiza un checkbox por cada valor de `ZONAS_DANO` (import de `src/features/casos/types.ts`) con su etiqueta legible (capitalizada), en una grilla de 2 columnas que no se rompe en tablet. No inventes una zona nueva ni omitas ninguna de las 10 — es la lista cerrada de D-17, no una sugerencia.

    Escribí `src/features/casos/components/FotoUploader.tsx`: recibe `caseId: string`, `angulos: readonly string[]` (para que el plan 02-03 lo reuse con los 4 ángulos `ingreso-*`) y `onUploadedCountChange(count: number)`. Por cada ángulo, un input de archivo (`accept="image/*"`, con atributo `capture="environment"` para preferir la cámara trasera en tablet/celular) que al seleccionar un archivo llama a `useCasoFotos().uploadFoto(caseId, angulo, file)`, muestra un preview (URL local con `URL.createObjectURL`, no la URL firmada — evitá pedir una URL firmada extra solo para el preview inmediato) y un estado de "subiendo…". Notificá la cantidad de ángulos con foto subida vía `onUploadedCountChange` cada vez que cambie.

    Escribí `src/features/casos/FichaInspeccionPage.tsx`: lee el `id` de caso de la URL, pide el caso con `getCaso(id)`, y renderiza `DamageCheckboxes` + `FotoUploader` (con `angulos={ANGULOS_FOTO}`) + dos botones. "Guardar ficha de inspección" queda deshabilitado mientras el conteo de fotos subidas sea menor a 4 (FICHAS-04); al presionarlo, persistí `danos_zonas` con `updateCasoEstado(id, caso.estado, { danos_zonas: zonasSeleccionadas })` — el estado no cambia todavía en este paso, solo se completan los datos de la ficha — y marcá un estado local `fichaGuardada = true`. "Marcar como enviado a la aseguradora" (D-15) queda deshabilitado hasta que `fichaGuardada` sea `true`; al presionarlo llama a `updateCasoEstado(id, 'enviado a la aseguradora')` y redirige a `/casos`. Este botón nunca dispara un envío de mail: la copy que lo acompaña dice explícitamente "Mandá el mail a la aseguradora desde tu correo con estos datos, y después marcá como enviado" — mostrá `numero_siniestro` y `denuncia` en un bloque copiable (texto seleccionable, no un botón de "copiar" con clipboard API, que agrega una dependencia de permisos innecesaria para un dato que se selecciona a mano).

    Escribí `FichaInspeccionPage.test.tsx` mockeando `api.ts` y `useCasoFotos`, cubriendo el bloque `<behavior>` completo: 10 zonas, 4 dropzones, el gate de 4 fotos sobre "Guardar", y el gate de `fichaGuardada` sobre "Marcar como enviado".
  </action>
  <verify>
    <automated>npx vitest run src/features/casos/FichaInspeccionPage.test.tsx && npx tsc --noEmit && npm run build && grep -c "ZONAS_DANO" src/features/casos/components/DamageCheckboxes.tsx && grep -c "ANGULOS_FOTO" src/features/casos/FichaInspeccionPage.tsx</automated>
    <human-check>
      Como recepción, creá un caso de Seguro real y completá la Ficha de inspección:
      1. Con menos de 4 fotos subidas, "Guardar ficha de inspección" tiene que verse deshabilitado.
      2. Subí las 4 fotos (podés usar las mismas 4 imágenes de prueba para los 4 ángulos): el botón se habilita.
      3. Guardá, y confirmá que "Marcar como enviado a la aseguradora" recién ahora se habilita.
      4. Marcalo, y confirmá en el listado /casos que el estado del caso cambió a "enviado a la aseguradora".
    </human-check>
  </verify>
  <acceptance_criteria>
    - `FichaInspeccionPage.test.tsx` pasa con los 4 comportamientos del bloque `<behavior>` cubiertos.
    - `DamageCheckboxes` renderiza exactamente 10 checkboxes, uno por cada valor de `ZONAS_DANO`.
    - `FotoUploader` renderiza exactamente 4 dropzones cuando se lo llama con `ANGULOS_FOTO`.
    - El botón de guardar la ficha queda inhabilitado con menos de 4 fotos subidas; el botón de marcar enviado queda inhabilitado hasta guardar la ficha.
    - Verificación humana: el flujo completo de subir 4 fotos → guardar → marcar enviado → ver el estado actualizado en /casos funciona en la app real.
    - `npx tsc --noEmit` y `npm run build` terminan en 0.
  </acceptance_criteria>
  <done>Un caso de Seguro puede completar su Ficha de inspección pre-ingreso con zonas dañadas y 4 fotos obligatorias, y avanzar a "enviado a la aseguradora" sin que el sistema mande ningún mail.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| formulario de alta → tabla `casos` | Los valores de `aseguradora` y `canal` llegan del cliente; el `<select>` cerrado es UX, el CHECK constraint del plan 02-01 es el control real |
| botón "Marcar como enviado" → percepción del usuario | El botón no manda ningún mail; su copy tiene que dejar eso inequívocamente claro para no generar la falsa expectativa de automatización que D-15 descartó explícitamente |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-2-06 | Tampering | `aseguradora` enviada desde el cliente | mitigate | `<select>` cerrado a las 6 opciones en la UI (task 1) + CHECK constraint en la base (plan 02-01); ambas capas, ninguna confía solo en la otra |
| T-2-07 | Repudiation | acción "Marcar como enviado a la aseguradora" | accept | En esta fase no hay registro de auditoría por acción individual (columna `updated_at` alcanza); si la Fase 5 necesita trazabilidad más fina para el reclamo a la compañía, se agrega ahí |
| T-2-08 | Spoofing (de capacidad, no de identidad) | botón "Marcar como enviado" | mitigate | La copy dice explícitamente que el mail lo manda la persona a mano, nunca sugiere automatización — D-15 |
</threat_model>

<verification>
1. `npx vitest run` completo en verde, incluidos los 3 archivos de test de este plan.
2. `npx tsc --noEmit` y `npm run build` en 0.
3. Verificación humana: alta de caso, ficha de inspección con 4 fotos, y marcar enviado, de punta a punta en la app real.
</verification>

<success_criteria>
- Recepción crea un caso de Seguro real con todos los campos de D-18/D-19.
- La Ficha de inspección pre-ingreso no se puede guardar sin las 4 fotos obligatorias.
- El caso avanza a "enviado a la aseguradora" sin ningún envío automático de mail.
- El botón "Nuevo caso" de RecepcionHome deja de estar deshabilitado.
- Los 2 tasks quedaron commiteados y `02-02-SUMMARY.md` escrito.
</success_criteria>

<output>
Create `.planning/phases/02-caso-de-seguro/02-02-SUMMARY.md` when done
</output>
